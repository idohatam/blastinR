% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_blast_db.R
\name{make_blast_db}
\alias{make_blast_db}
\title{Create a BLAST Database from a FASTA File}
\usage{
make_blast_db(
  infile = file.choose(),
  dbtype = "nucl",
  outfile = NULL,
  taxids_file = NULL,
  report = TRUE
)
}
\arguments{
\item{infile}{A character string specifying the input FASTA file path.
If not provided, a file chooser dialog will open.}

\item{dbtype}{A string specifying the type of database to create.
Default is \code{"nucl"}; other option is \code{"prot"}.}

\item{outfile}{A string specifying the base name for the output database files.
If not supplied, the function will derive it from the input file name
by removing \code{.fa}, \code{.fasta}, or \code{.txt}.}

\item{taxids_file}{(Optional) A path to a taxonomy mapping file.
The file should be tab-delimited with two columns: sequence ID and taxonomy ID,
matching the format used by NCBI.}

\item{report}{Logical, default \code{TRUE}. If \code{TRUE}, a report entry is added via internal logging.}
}
\value{
A character vector with a success message or any error messages encountered during database creation.
}
\description{
This function wraps the BLAST+ \code{makeblastdb} command to create a BLAST database
from a FASTA-formatted input file. Optionally, taxonomy information can be added
using a tab-delimited taxonomy mapping file. A basic report can also be generated
for reproducibility.
}
\details{
This function assumes that the BLAST+ suite, specifically the \code{makeblastdb}
executable, is correctly installed and available on the system PATH.
You can check installation status using \code{\link[=check_blast_install]{check_blast_install()}}.
}
\examples{
\dontrun{
# Example: Create a nucleotide BLAST database from a FASTA file
make_blast_db(infile = "sequences.fasta", dbtype = "nucl", outfile = "my_blast_db")

# Example with taxonomy mapping file
make_blast_db(infile = "sequences.fasta",
              dbtype = "nucl",
              outfile = "my_blast_db",
              taxids_file = "taxonomy_map.txt")
}
}
\seealso{
\code{\link[=check_blast_install]{check_blast_install()}}, \code{\link[=blstinr]{blstinr()}}
}
