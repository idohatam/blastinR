% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blstinr.R
\name{blstinr}
\alias{blstinr}
\title{Run BLAST+ Searches from Within R}
\usage{
blstinr(btype = "blastn", dbase, qry, taxid = FALSE, numt = 1, ...)
}
\arguments{
\item{btype}{Character string specifying the BLAST tool to use. Defaults to \code{"blastn"}.}

\item{dbase}{Character string giving the path to the BLAST database.}

\item{qry}{Character string giving the path to the query FASTA file.}

\item{taxid}{Logical. If \code{TRUE}, includes taxonomic IDs in the output. Defaults to \code{FALSE}.}

\item{numt}{Integer. Number of threads to use for BLAST. Defaults to \code{1}.}

\item{...}{Additional arguments passed to \code{system2()}, if needed.}
}
\value{
A tibble containing BLAST results, including columns for sequence identifiers, alignment metrics,
and optionally taxonomic information. A CSV of the results is saved to \code{outputs/table/}.
}
\description{
This function allows users to run various BLAST+ search types (e.g., \code{blastn}, \code{blastp}) directly from R.
It wraps a system call to the BLAST+ command-line tools and processes the output into a tidy tibble,
optionally including taxonomic identifiers and saving the results to disk.
}
\details{
This function requires the BLAST+ suite to be installed and accessible via the system \code{PATH}.
The output is parsed into a tibble and augmented with a column \code{Range} representing alignment length.
Internal helper functions handle directory setup, timestamping, and report logging.
}
\examples{
\dontrun{
blstinr(btype = "blastn", dbase = "nt", qry = "query.fasta")
blstinr(btype = "blastp", dbase = "swissprot", qry = "protein.fasta", taxid = TRUE, numt = 4)
}

}
